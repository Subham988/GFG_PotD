
int solve(int a, int b, int c) {
        // code here
	// this is subham
        int maxi=max(a,max(b,c));
        int mini=min(a,min(b,c));
        int mid=a+b+c-maxi-mini;
        if(maxi<=(mid+mini+1)*2)
            return a+b+c;
        else return -1;
    }